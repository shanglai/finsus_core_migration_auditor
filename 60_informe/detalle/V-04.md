# V-04 · Rendimiento vista — oraculo independiente

> Ficha detallada del punto de validacion. Complementa
> `40_validaciones/PAQUETE_AUDITOR_DATOS/01_TABLA_MAESTRA_VALIDACIONES.md` (alto nivel).
> Generado por `60_informe/generar.py` — no editar a mano: editar `60_informe/puntos.py`.

| | |
|---|---|
| **Familia** | Captacion |
| **Motores comparados** | C vs B |
| **Corte de datos** | 2026-08-28 |
| **Ejecutado (corrida)** | 2026-08-28 |
| **Caso ejecutable en el validador** | `REND-VISTA` |
| **Solicitudes abiertas** | SOL-003 |

## 1. Alcance

### Que SI se valida

El interes de cuenta a la vista calculado de forma independiente (SPM x tasa x dias / base) contra lo que AurumCore posteo.

### Que NO se valida

Se declara explicitamente para que el resultado no se lea con mas cobertura de la que tiene.

- El SPM de RENDIMIENTO en si: se reconstruye de `finsus_account_history`, no se lee del insumo real que uso el core (ese vive en la poliza de intereses, SOL-003).
- El primer cierre mensual de vista post-cutover — se observa el 31-ago.

## 2. Periodo

| | |
|---|---|
| **Ventana de datos** | cierre de agosto 2026 sobre `yield_dto` + `finsus_account_history` |
| **Fecha de corte** | 2026-08-28 |
| **Cuando se corrio** | 2026-08-28 |

> La fecha de corte es **con que datos** se valido; la de corrida es **cuando se ejecuto**. Pueden diferir y se reportan por separado.

## 3. Universo y representatividad

| | |
|---|---|
| **Comparado (n)** | **20,000** pagos de rendimiento vista |
| **De un total de** | [PEND] |
| **Segun que fuente** | B (AurumCore) |
| **Representatividad** | **[PEND]** |
| **Conciliado contra** | PEND |

El limite de 20,000 es una COTA DE LA EXTRACCION (`limite` del caso), no el universo. Hay que declarar cuantos pagos hubo ese dia para expresar la representatividad.

> **Este denominador esta PENDIENTE.** Es la pregunta que la auditoria dejo abierta en la sesion del 2026-08-28 [00:32:35]: *"cuanto representan esos items respecto del universo"*. Se cierra con:

```sql
select count(*) from aurumcore.yield_dto y where y.iv_payment_plan_id is null and y.process_date = :fecha_pago
```

## 4. Racional del subconjunto

*Por que este recorte y no otro* — la otra mitad de lo que pidio la auditoria: *"la metodologia con la que determinaron cuantos y por que"*.

El tope de 20,000 filas es deliberado y su motivo esta declarado: no degradar AurumCore, que es PRODUCCION [transcripcion 00:28:23 y 00:30:42]. La herramienta NO muestrea por diseño —puede correr el universo completo— y el limite se sube en cuanto se acuerde la ventana de ejecucion. Es una restriccion operativa declarada, no una decision estadistica.

## 5. Santo y sena — como se reproduce

| | |
|---|---|
| **Tablas / fuentes** | `aurumcore.yield_dto`, `aurumcore.finsus_account_history`, `aurumcore.account` |
| **Llave de comparacion** | `account_id` |
| **Formula (motor C)** | `Rendimiento = Round2( Trunc20( Trunc20((SPM x Tasa)/100) / DiasAnio ) x DiasPeriodo )` |
| **Tolerancia** | 0.01 por evento, con prueba de signo |
| **Oraculo** | `oraculos/rendimientos.py::fila_rendimiento_vista` |
| **SQL** | [`validador/extraccion/aurum/rend_vista_universo.sql`](../../validador/extraccion/aurum/rend_vista_universo.sql) |

**Predicados exactos que definen el universo:**

```sql
y.iv_payment_plan_id is null  -- VISTA, no inversion
y.process_date = :fecha_pago
h.record_date = :fecha_cierre
```

**Reproducir:**

```bash
cd validador && python cli.py --caso REND-VISTA --confirmar
```

## 6. Resultado

96.62% al centavo (19,325 / 20,000)

**Cuadre por granularidad:** 1e-8 96.37% · 1e-5 96.37% · centavo 96.62%

> El **escalon** entre niveles es diagnostico: bajo a 1e-8 y alto al centavo = residuo sub-centavo (granularidad del snapshot, no defecto); bajo tambien al centavo = diferencia material. Ver `MATRIZ_TOLERANCIAS.md`.

**Prueba de signo:** ver corrida — el residuo se concentra en cuentas con proxy de fecha de activacion

**No conformes** — clase `data-sourcing`:

675 al centavo. De ellos 398 son diferencias SUB-PESO. Solo 8 superan $100, y las 8 tienen el mismo patron: la fecha de activacion usada como proxy sobrecuenta dias (fondeo != activacion).

## 7. Contraste con el informe detallado de Linko

> Este tablero es un tercero: donde su medicion y la del repo de validacion no coinciden, se dice — no se alinea en silencio.

NO SON COMPARABLES Y HAY QUE DECIRLO. Esta corrida es del CIERRE DE AGOSTO sobre una cota de 20,000 filas y da 96.62% al centavo. El informe de Linko reporta el CICLO DE JULIO como CENSO de 83,094 cuentas (~100% de los pagadores del ciclo, de 915,016 cuentas vista, la mayoria con interes 0) con 94.76% a 1e-8 y 95.03% al centavo, con `oraculo_vista_finsus_history.py`, base 360 y dt 31. Ciclos y universos distintos: ni se contradicen ni se promedian. Ademas `MATRIZ_TOLERANCIAS.md` mantiene VISTA en [PEND] A PROPOSITO porque se sella con el ciclo vivo del 31-ago; por eso el tablero muestra su cifra con fecha y nota y no como si cerrara el punto (INV-C3).

## 8. Lo que este punto NO concluye

Los limites de la afirmacion. Un resultado leido fuera de estos limites dice mas de lo que la prueba soporta.

- El residuo NO se puede atribuir a AurumCore hasta tener el SPM real y el `dt` de la poliza de intereses (SOL-003). El oraculo reconstruye el SPM, y una diferencia contra un insumo reconstruido no acusa al motor.

## 9. Evidencia

- `validador/reportes/REND-VISTA_2026-08-28_*/`

---

*Verde no es dictamen. Cada validacion devuelve las filas que violan la regla; cero filas significa cero violaciones en ESTE universo, no que el motor este bien fuera de el. El dictamen lo emite el humano.*
