# V-03 · Rendimiento vista — integridad de posteo (feed ↔ DB)

> Ficha detallada del punto de validacion. Complementa
> `40_validaciones/PAQUETE_AUDITOR_DATOS/01_TABLA_MAESTRA_VALIDACIONES.md` (alto nivel).
> Generado por `60_informe/generar.py` — no editar a mano: editar `60_informe/puntos.py`.

| | |
|---|---|
| **Familia** | Captacion |
| **Motores comparados** | feed vs B |
| **Corte de datos** | 2026-08-18 |
| **Ejecutado (corrida)** | 2026-08-23 |
| **Caso ejecutable en el validador** | no hay — este punto se cita del repo de validacion |

## 1. Alcance

### Que SI se valida

Que cada pago de rendimiento que el core escribe en su feed operativo (`yield-trans`) EXISTA en la base con la misma cuenta y el mismo monto. Es integridad de POSTEO, no correccion del CALCULO.

### Que NO se valida

Se declara explicitamente para que el resultado no se lea con mas cobertura de la que tiene.

- El calculo del rendimiento — eso es V-04, con oraculo independiente.
- Los pagos de la DB que NO aparecen en el feed: el feed capturado es de UN pod, asi que la direccion contraria (DB ⊆ feed) no se afirma y no se puede afirmar.

## 2. Periodo

| | |
|---|---|
| **Ventana de datos** | 2026-08-18 (dia completo en DB; feed parcial de un pod) |
| **Fecha de corte** | 2026-08-18 |
| **Cuando se corrio** | 2026-08-23 |

> La fecha de corte es **con que datos** se valido; la de corrida es **cuando se ejecuto**. Pueden diferir y se reportan por separado.

## 3. Universo y representatividad

| | |
|---|---|
| **Comparado (n)** | **30,769** pagos capturados en el feed |
| **De un total de** | 38,921 |
| **Segun que fuente** | B (AurumCore) — pagos del dia completo en DB |
| **Representatividad** | **79.06%** |
| **Conciliado contra** | SI se concilio: 38,921 pagos · $5,751,013.03 en la DB del dia, productos 2301/2307/2308. El feed es subconjunto propio y coincide 30,769/30,769. |

El feed capturo 30,769 de los 38,921 pagos del dia porque se leyo UN pod. La direccion validada es feed ⊆ DB al 100%.

## 4. Racional del subconjunto

*Por que este recorte y no otro* — la otra mitad de lo que pidio la auditoria: *"la metodologia con la que determinaron cuantos y por que"*.

El recorte no se eligio: lo impone la captura. El feed se lee de los logs de UN pod del core, y el core corre en varios. Se valida la direccion que el dato SOPORTA (todo lo que el feed dice esta en la DB) y se declara que la contraria no se puede afirmar con esta captura.

## 5. Santo y sena — como se reproduce

| | |
|---|---|
| **Tablas / fuentes** | `feed yield-trans (CSV pre-extraido)`, `aurumcore transaccional del dia` |
| **Llave de comparacion** | `payee_account_id + monto` |
| **Formula (motor C)** | `n/a — es cruce de existencia, no de calculo` |
| **Tolerancia** | existencia exacta |
| **Oraculo** | `cruce de conjuntos` |

**Predicados exactos que definen el universo:**

```sql
fecha = 2026-08-18
productos 2301 / 2307 / 2308
```

## 6. Resultado

feed ⊆ DB = 30,769 / 30,769 = 100.00%

**No conformes**:

ninguno en la direccion validada

## 7. Lo que este punto NO concluye

Los limites de la afirmacion. Un resultado leido fuera de estos limites dice mas de lo que la prueba soporta.

- NO prueba que el core haya posteado todos los pagos que debia: prueba que lo que el feed reporta esta en la base. La completitud absoluta requiere el feed de todos los pods.

## 8. Evidencia

- `yield_feed_2026-08-18.csv`
- `RESULTADO_rendimiento_feed_2026-08-23.md`

---

*Verde no es dictamen. Cada validacion devuelve las filas que violan la regla; cero filas significa cero violaciones en ESTE universo, no que el motor este bien fuera de el. El dictamen lo emite el humano.*
