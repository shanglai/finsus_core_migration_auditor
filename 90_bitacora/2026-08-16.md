# Bitácora — 2026-08-16

## Sesión — Ingesta F-012 (queries Aurum) y F-013 (árbol de decantación día cero)
`landing/` traía el `Inventario Queries.xlsx` y el folder completo `Análisis árboles` (120 archivos,
~1.5 GB).

### Fuentes
- **F-012** `Inventario_Queries_AurumCore.xlsx`: 5 queries SQL de AurumCore → revelan su modelo de
  datos (esquema `aurumcore`). **Cierra buena parte de P-011.** Se versiona (SQL, sin datos).
- **F-013** `analisis_arboles_20260803/`: reconciliación OpenFin vs Aurum por dominio al corte
  02-03 ago (En común/Único/Diff + RCA + Asignaciones). **Datos crudos ~1.5 GB con PII →
  gitignored**; traza en `..._MANIFEST.md`. Es reconciliación A/B, no arbitraje C.

### Piezas (6) → total 33; actualizada K-DAT-002 v2
K-DAT-006 (modelo/queries Aurum), K-MIG-005 (árbol metodología), K-CAP-001 (cuentas vista),
K-COL-001 (One Click cuadra 100%), K-FIS-003 (Diff ISR mayor gap), K-MOV-007 (transacciones).
K-DAT-002 v2: nombres físicos de columnas de OpenFin confirmados por extracciones reales.

### Artefactos
- `00_entendimiento/ANALISIS_ARBOLES.md` (entendimientos individuales por dominio + lectura transversal).
- `PLAN_DE_VALIDACION.md` §"4.bis Estado real al corte" (se sumó el árbol al plan).
- 7 candidatos a hallazgo nuevos (A13-*): ISR, BUG API, tasa 2019, saldo ingesta, SPEI satélites, etc.

### Entendimiento (v5 → v6, ~52% → ~62%)
- **Modelo de Aurum** mapeado (P-011 casi cerrada); nombres físicos de OpenFin (P-004 casi cerrada);
  P-009 (cifras día cero) **cerrada** con datos reales.
- Hallazgos de negocio: **One Click cuadra 100%** (señal fuerte); **ISR de inversiones es el mayor
  gap (≈27%)**, en parte cascada del diff de saldo.

### Decisiones / notas
- Datos crudos NO se pushean (PII + tamaño) — refuerza [[push-confidencial-finsus]].
- El árbol es A/B con RCA auto-reportado; el trabajo de C es re-derivar los cálculos discrepantes.

## Acceso a AurumCore concedido (tarde 16-ago)
`\d` de la base `aurumcore` (26 relaciones). K-DAT-006 v2. Banderas detectadas:
- Duplicados aparentes: `accountholder` vs `account_holder`, `account` vs `investment`,
  `payment_plan` vs `iv_payment_plan` (¿doble modelo / migración?).
- La query F-012 usa `"transaction"` pero la tabla real es `transaction_detail` (verificar).
- `fns_history_tiiemxn` (TIIE, tasa referencia), `tmp_linaje_*`/`tmp_migracion_linaje` (linaje de
  ingesta → K-MIG-002), tablas de prueba en prod (`test/z_test/ztest`).
Pendiente: `\d+` de las tablas núcleo. Accesos a OpenFin t-1 y Reportes Unificados aún en gestión.

## Próxima sesión
- (Procesar `landing/` primero si hay algo.)
- Arrancar el oráculo por **ISR de inversiones** (mayor gap) sobre el mismo saldo base, para separar
  defecto-de-ISR de defecto-de-saldo. Requiere cerrar P-010 (parámetros normativos).
- Pedir columnas de `aurumcore."transaction"` (resto de P-011).
- Diseñar Fase 1 (correlación/exclusiones) con las llaves reales OpenFin↔Aurum (K-DAT-006).
