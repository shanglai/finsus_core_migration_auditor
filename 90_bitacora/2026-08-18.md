# Bitácora — 2026-08-18

## ISR paso a paso (A/B/C) + arranque de Fase 1 (extracción BD)

### Análisis offline (sin BD)
- Se explicó cómo calcula **C** el ISR (K-FIS-002/S-FIS-001) y se generó
  `_resultados/ISR_A_B_C_ejemplos.xlsx` (caso de oro: C=B al centavo, 46.37/4.81/0.05).
- Se generó `_resultados/ISR_paso_a_paso_A_B_C.xlsx` con **ledgers día-por-día de C** para 3 casos:
  - Caso A (oro, OF<AC): C=B=46.37; OpenFin 3.93 = snapshot parcial.
  - Caso B (OF≫AC expuesto, `1-10-370`): C recalculado **765.76 ≈ AurumCore 765.75**; OpenFin **1,421.15** (1.86×).
  - Caso C (OF≫AC exento): AurumCore=0 (cliente < 5×UMA), C=0, **OpenFin 189.67** → sobre-retención.
- **Hallazgo candidato:** de 18,599 inversiones comparables, **1,710 con OpenFin > AurumCore** (>$0.10);
  de esas **257 con AurumCore=0** (cliente exento) y OpenFin > 0 → sobre-retención histórica.
  Verificado que los 257 clientes tienen base < exención. Candidato **DEFECTO_OPENFIN** (charter §11).
- Precisión: el cliente de oro tiene **vista** (Σplazo 139k < exención pero base real 311k > exención);
  el tipo-C se sostiene en **AC=0 ⇒ base total < exención ⇒ C=0**, no en Σplazo.

### Código de Fase 1 (extracción BD) — generado, NO ejecutado
- Columnas reales confirmadas en diccionarios (F-014/F-015) antes de escribir SQL (nada inventado):
  OpenFin `isr_diario(fecha,kasociado,saldo,isr)` (cliente-día), `asociados(kasociado)`,
  `isr_diario_aux_log(kauxiliar)`; Aurum `cat_tax`, `account_tax(isr,isr_concept)`,
  `account_yield(isr_exempt,days_in_year)`, `account_balance_tracking`, y que el ISR al pago se
  asienta como **transacción** (`transaction_detail.debit_amount`) → hay que **descubrir** el tipo.
- `40_validaciones/PLAN_FASE1_ISR.md`: actividades (A1–A9), rangos/medidas, pasos (P0–P7), bullets a Finsus.
- SQL solo-lectura, columnas nombradas, acotados: `00_volumetria_isr.sql`, `openfin_isr_diario.sql`,
  `aurum_cat_tax.sql`, `aurum_account_yield.sql`, `aurum_isr_al_pago_discovery.sql`,
  `aurum_isr_al_pago.sql`, `aurum_saldo_base_isr.sql`.
- Runner `fase1_isr_runner.py` (**solo lectura, gated**; `--dry-run` default, `--confirm` para conectar;
  cohorte como CTE VALUES → sin temp tables; DSN por env `OF_DSN`/`AC_DSN`) + comparador
  `fase1_isr_comparador.py` (C con Decimal, A/B/C + clasificación). Probados en dry-run OK.

### PII / repo
- Sin ids de cliente en archivos versionados: la semilla se deriva del parquet gitignored o de
  `_resultados/cohorte_semilla.txt` (gitignored). Exceles en `_resultados/` (gitignored). Nada pusheado.

### P1 ejecutado contra BD (solo lectura) — hallazgos
Conexión vía `db_connections.yaml` (gitignored). Usuario `dlopez_linko`. Corrida real, read-only.

**1. [CONFIRMADO] Bloqueo de acceso en OpenFin (openfin_aurum) — motor A bloqueado.**
`dlopez_linko` NO tiene `GRANT SELECT` sobre ninguna tabla de datos de `public`:
`asociados`, `acreedores`, `isr_diario`, `isr_diario_aux_log`, `detalle_auxiliar`,
`detalle_auxiliar_masdatos`, `calculo_intereses_acreedores` → todas **permiso denegado**
(solo el catálogo `pg_class` responde). `isr_diario` confirmada en `public`, **171,829,856 filas**.
→ El día-por-día de A no se puede extraer hasta que Finsus otorgue el grant.

**2. [CONFIRMADO] AurumCore: acceso completo** a las 9 tablas necesarias (account, accountholder,
account_balance_tracking, transaction, transaction_detail, cat_tax, account_tax, account_yield, iv_payment_plan).

**3. [CONFIRMADO] La llave de cliente en Aurum NO es `100-10-X`.** `accountholder.external_id` es
un entero interno (p.ej. `2066160`); no existe columna `accountholder_number`. El `[?]` del mapeo
estaba mal. Sí existe `account.account_number` = `100-2301-X`. → La cohorte en Aurum debe anclarse
por **`account_number`** (que sí tengo en el parquet) → `accountholder_id` → todas las cuentas del titular.
Falta verificar la ruta account→accountholder en vivo (la VPN cayó antes de confirmarlo).

**4. P-010 — parámetros ISR de AurumCore (config en vivo):**
- [CONFIRMADO] `system_configuration.tax.days.year = 365`.
- [CONFIRMADO] `yield.tax.exempt.uma.amount = 5` (multiplicador de la exención).
- [CONFIRMADO] `account_tax` con `isr_concept ∈ {ISR BASE, ISR}` → **isr = 0.90** (0.9%), `base_period_type=2`.
- [CONTRADICCION candidata] `system_configuration.yield.tax.exempt.amount = 206,367.60`
  (= 5 × 41,273.52 = 5 × UMA **2025** anual), **pero el caso de oro usó 213,973.20**
  (= 5 × 42,794.64 = UMA **2026** anual). Cambia la parte expuesta → cambia TODO el ISR de C.
  Registrar en CONTRADICCIONES y confirmar con TODAS las filas de `exempt.amount` (VPN cayó antes).

### VPN reactivada · P1-AC / P2 / P3 ejecutados (solo lectura)

**Correcciones de código (probadas en vivo):**
- Llave de cohorte Aurum reanclada a **`account_number` → `accountholder_id` → todas las cuentas**
  del titular (subconsulta contra `cohorte_acc`). Confirmado: 4 cuentas semilla → 4 titulares → 48 cuentas.
- Bug del runner: el split por `;` rompía en un `;` dentro de comentarios → ahora se quitan comentarios
  antes de partir. Queries AC de extracción reescritas (`00_volumetria_ac`, `aurum_saldo_base_isr`,
  `aurum_isr_al_pago`, `aurum_isr_al_pago_discovery`).

**P1-AC volumetría:** account_balance_tracking del cohorte = 65 filas / 16 cuentas / 4 titulares,
fechas 2025-10-16 → 2026-08-03. `account_tax`: 12 esquemas ISR (0.50–10.80, incl. 0.90 'ISR BASE').

**P3 · [CONFIRMADO] Asiento del ISR al pago en AurumCore (firma verificada):**
`transaction_type='INTERNAL TRANSFER'` + `transaction_channel='Generic'`; débito en la cuenta VISTA
del cliente (`100-2002-X` / `1-2002-X`), **crédito a la cuenta de ISR `100-0000-438220`** (producto
`0000`). `isr_retenido = credit_amount`. Comprobado en semilla: `1-10-370 → 765.75`,
`100-10-677746 → 137.04`; los exentos (`14083`, `517225`) **sin fila** (correcto). Fecha 2026-08-03.
  → `aurum_isr_al_pago.sql` fija esta firma. (Nota: `alfanumeric_reference='Cobro de ISR'` de
  `system_configuration` NO aparece en `transaction_detail`; la referencia real es 'Pago de rendimientos-...').

**B (ISR al pago) extraído** para semilla: 2 filas (765.75, 137.04) → coinciden con los valores conocidos.
**Base de saldos** extraída: 65 filas. Pipeline read-only → Parquet validado end-to-end en datos reales.

### C-001 resuelta (con evidencia) + C=B sobre datos reales
Cliente `1-10-370`: 1 sola inversión viva el 2026-08-03 (`1-2301-14994`, 300,000, activada
2025-08-07 → 361 días). Despejando la exención desde el ISR real posteado (765.75):
- exención despejada = 213,973.92 ≈ **213,973.20** (5×UMA 2026).
- C con 213,973.20 = **765.76** (= B 765.75, 1 centavo) · C con 206,367.60 = 833.46 (≠ B).
→ **AurumCore APLICA 213,973.20**; el `system_configuration.exempt.amount=206,367.60` (UMA 2025)
está **stale/no usado** (el motor calcula 5×UMA en vivo). C-001 ANALIZADA (ver CONTRADICCIONES);
falta escalar a Finsus el porqué del config viejo y verificar UMA 2026 vs INEGI.
**C = B confirmado sobre datos de BD** (no solo del Excel).

Mecánica adicional observada: **reinversión intradía** (p.ej. `100-10-14083` el 2026-08-03: la
inversión que maduró pagó con base ≤ exención → ISR 0, y se reinvirtió como `100-2301-10283266` =
221,049.14). Implica que la base del ISR para C es la **previa al pago**, no el saldo fin-de-día.
`account_balance_tracking` no trae los plazos de todos los clientes (p.ej. 1-10-370 sin filas) →
para C multi-inversión conviene usar `iv_initial_amount` + fechas `activation/iv_closing`.

### GRANT otorgado · Motor A (OpenFin isr_diario) extraído y comparado
Acceso OK a las 7 tablas de `public`. `isr_diario`: 171.8M filas, rango **2025-09-03 → 2026-08-17**.

**Extracción A (semilla, 182 días 2026-02-03→2026-08-03):** 728 filas. Σ ISR OpenFin por cliente:
`1-10-370`=1,880.54 · `677746`=1,288.52 · `517225`=195.60 · `14083`=120.08. (vs Aurum al pago: 765.75/137.04/0/0).

**[CONFIRMADO] `isr_diario` es DEVENGO/PROVISIÓN diario, no retención en efectivo.**
En `1-10-370`: acumula 2.12/día con saldo 300k, y el **día del pago (2026-08-03) el saldo→0 y el ISR→0**.
La retención real en efectivo es AL PAGO (como Aurum).

**[CONFIRMADO] El devengo diario de OpenFin sigue la regla:** = C (tasa/365 × (saldo−213,973.20)),
Σ|dif diaria|=5.87 en 728 días. OpenFin usa la MISMA exención aplicada que Aurum (213,973.20, no la
206,367.60 de config). Cola menor: 9 días de saldo alto (770k) se despegan +0.19/día (~exención 2025) — investigar.

**Conclusión de modelo:** A (Σ devengo) >> B (retención al pago) es **diferencia de MODELO** (devengo diario
acumulado vs retención al pago), NO necesariamente sobre-retención. **Pendiente decisivo:** reconciliar
Σ devengo de A sobre la VIDA de cada inversión vs lo retenido por Aurum al pagarla (usar `isr_diario_aux_log`
por cuenta + todos los pagos de Aurum del cliente). Si coinciden → solo momento; si divergen → hallazgo.

Entregable: `_resultados/ISR_dia_por_dia_REAL_semilla.xlsx` (día-por-día real A/C/B). Pipeline completo
A+B+C sobre BD, solo lectura.

### Reconciliación por vida de inversión (EJECUTADA) — el "OF≫AC" se explica
Inversión `1-2301-14994` (300k, 361 días, cliente 1-10-370, OF kauxiliar 6154866):
- Σ `isr_diario_aux_log` (devengo diario, vida) = **1,421.15** = **exactamente el `isr_of` del árbol**.
  → El "ISR OpenFin" del árbol es la **PROVISIÓN/devengo diario**, no una retención. [CONFIRMADO]
- Aurum retención al pago = **765.75** = regla sobre capital propio (C=765.76). [CONFIRMADO]
- OpenFin `calculo_intereses_acreedores.provisionar=True` → OpenFin **provisiona** ISR diario. [CONFIRMADO]
- Retenciones ISR en Aurum **sólo desde 2026-08-02** (cutover). Es tema de migración. [CONFIRMADO]
- Consistencia OF: Σ aux_log (todas las cuentas) en 2026-02..08 = 1,880.54 = isr_diario cliente ✓.

**Lectura:** el "OpenFin ≫ AurumCore" del árbol **NO es sobre-retención**: compara provisión-devengo (OF)
vs retención-al-pago (AC), dos constructos distintos. **Se degrada de hallazgo a artefacto de comparación.**

**Riesgo real que emerge (H-J, prioridad alta):** OpenFin `calculo_intereses_acreedores` (prod 2301)
config **efectiva 2026-08-31: tasa_ret=1.45%, top_isr=158,469** ≠ Aurum (0.9% / 213,973.20). En el
periodo observado ambos coincidían (~0.9%/213,973); la divergencia es **hacia adelante** → si se aplica,
cambia el ISR de todo el padrón. Escalar.

**Abierto para veredicto formal:** retención REALIZADA de OpenFin (no provisión) de una inversión pagada
pre-cutover; reconciliación cliente en ventana común; base normativa de exención + UMA 2026 (INEGI); C-001.

Reporte detallado actualizado: `_resultados/REPORTE_FASE1_ISR.md` (§9 reconciliación + hipótesis/evidencia).

### Ingesta F-016 (motores AurumCore) + F-017 (conversación Finsus post-Fase 0)
Material movido de `landing/` a `20_fuentes/` (docs/motores gitignored — confidenciales; v2t gitignored).
`landing/` y `20_fuentes/docs/**/*.pdf` añadidos a `.gitignore`.

**F-016 · "Cálculo de Pago de Rendimientos" (spec oficial v1.0, 7-ago-2026) — confirma el oráculo C:**
- [CONFIRMADO] ISR **sólo al pago**, no durante devengamientos (literal). Saldo total = Σ vista (saldo
  promedio) + Σ plazo (capital inicial). Exención = UMA×5, "ago-2026 ≈ 213,973.20", personas morales 0.
  Días año 365 ("la normativa menciona 365"). Rendimiento plazo = capital×tasa/100×días/360 half_even.
- [CONFIRMADO] Fórmulas ISR con `Trunc20`/`Trunc5`/`Round2` → afinan la precisión de S-FIS-001.
- **[CONTRADICCION C-002]**: el doc escribe `proporción = saldo_cuenta / base_gravable`, pero F-010 y la
  BD real usan `÷ saldo_total`. El doc **sobre-retendría** (1-10-370: 2,670.41 vs real 765.75). C usa
  ÷saldo_total (correcto). Escalar a Finsus. → K-FIS-002 v2, CONTRADICCIONES C-002.
- Saldo Promedio PDF: algoritmo (acumulado histórico + saldo×días / días transcurridos), `difference of
  days` (exclusivo) vs `elapsed days` (inclusivo), base 360/365 **por producto**; se valida en logs del
  CORE (`Calculating with average balance`) → conecta con `logs_core_rendimientos` que ya tenemos.
- Ciclos Transaccionales (dominio MOV): flujos SPEI IN/OUT, Pomelo, P2P, reversas/refund/cancel + concili.
  con Pomelo. Intereses de Créditos (dominio COL, One Click): pendiente de procesar a detalle.

**F-017 · conversación Finsus (post-Fase 0):**
- Piden **validar nuestra interpretación de Fase 0** (sesión mañana 9am). Trazabilidad: AurumCore la
  tiene, OpenFin hay que reconstruirla desde logs (coincide K-MOV-006); Abraham (experto OpenFin) apoya.
- Independencia reforzada (cuadrar sin pasar datos de un core al otro). Objetivo: **"un mes real" con
  evidencia**, 3 semanas, proyecto aislado al equipo técnico; el Consejo ya sabe de los terceros indep.
- Movimientos no cuadrarán (SPEI rechazado/payout doble en OpenFin) → explicable, "probar desde 18-ago".
- Mapa de 5 bloques: operaciones · motores de cálculo · contrapartes (PLD/RedCheck, cobranza F1, conta) ·
  migración/cuadre (decantación) · reportes (23/29 liberados).

**Pendiente propagar:** revisar S-FIS-001 (fijar ÷saldo_total y guardar C-002; añadir precisión Trunc20/5).

### Prueba a escala del set de desviación de ISR — CERRADO (lado cálculo)
`fase1_isr_desviacion.py` (solo lectura, gated) sobre TODO el set material |A−B|>0.10:
**3,236 inversiones / 2,774 clientes → 3,236 MODELO, 0 REVISAR.**
- El devengo diario de OpenFin sigue la regla (aceptando UMA 2026 o 2025 en la transición); AurumCore
  retiene al pago (C=B). El "Diff ISR" del árbol (13,584 cuadran / ~5K desvían, **bidireccional 50/50**)
  es provisión-devengo vs retención-al-pago = artefacto de modelo, **no defecto**. → K-FIS-003 v2.
- Muestra previa (400): 399 MODELO + 1 borde (`100-10-36088`, frac 0.898) que el refinamiento UMA resolvió.
- Salvedad: `A_prov(BD)=Σ aux_log vida` ≠ `isr_of(árbol)` en 2/3 (el árbol es provisión de periodo acotado,
  no vida completa) — no afecta la conclusión (la clasificación se basa en isr_diario diario vs regla).
- Bug menor corregido (`isr_of`→`isr_of_arbol` en el print de sanidad).

**Cierre ISR-inversiones:** el CÁLCULO queda validado y el descuadre de Fase 0 explicado. Faltan para el
cierre FORMAL: **C-002** (proporción doc), **P-010** (verificación normativa UMA/tasa/exención), **H-J**
(parámetros go-forward). Los tres son preguntas a Finsus / verificación contra la norma, no cálculo.

### Material nuevo de Finsus (F-018/019/020) — 2026-08-19
- **F-019** (Pago de Rendimientos actualizado): **corrige la proporción del ISR a ÷ Saldo total** →
  **C-002 RESUELTA** a nuestro favor (el oráculo C ya usaba ÷saldo_total; no cambia nada en C). Residuo:
  el EJEMPLO del doc quedó inconsistente (sigue en ÷base_gravable, 22.93 en vez de 13.38) — avisar.
- **F-018** (puntos_validacion): checklist de motores 2.1.1–2.1.12. ISR (2.1.4) cerrado; roadmap para el
  resto (rendimientos vista/plazo, saldo promedio, crédito ord/mor, IFRS9, IVA, comisiones, CAT/GAT,
  seguros, límites transaccionales).
- **F-020** (GAP Analysis de Finsus): 5 **motores regulatorios FALTANTES** en AurumCore (write-offs;
  suspensión de devengo/IDNC en cartera vencida CNBV C-16; cuota Prosofipo/IPAB; tasas variables TIIE;
  revaluación ME/UDIS). Clase de hallazgo NUEVA (funcionalidad ausente, no diferencia de cálculo).
  **Son aserciones de Finsus citando normativa → verificar** (norma real + comportamiento real de Aurum);
  no dar por ciertas (§3.3). Potenciales go-live blockers si se confirman (esp. B suspensión devengo y C Prosofipo).

### Estado de conexión
VPN intermitente (varias caídas). Acceso completo a **AurumCore y OpenFin** cuando está arriba.

### Pendientes / siguiente
- **Reactivar VPN** y reintentar.
- **Pedir a Finsus GRANT SELECT** (read-only) sobre las tablas de datos de `public` en openfin_aurum
  (mín.: `isr_diario`, `asociados`, `acreedores`; idealmente el esquema para el resto de dominios).
- **Reescribir** las queries de Aurum para anclar por `account_number` → `accountholder_id` (no `external_id`).
- **Cerrar P-010**: confirmar el `exempt.amount` correcto (2025 vs 2026) y actualizar S-FIS-001 / K-FIS-002.
- Descubrir `:isr_txn_type` en Aurum (P3); luego escalar cohortes.
- Cargar los 257 tipo-C como ficha(s) `H-###`.
